# Permissions

| Permission | Description | Default |
|---|---|---|
| `swagbounties.admin` | Access to all `/bountyadmin` commands | op |

## Notes

- Regular players have no special permissions — `/bounty` and `/bounties` are open to all.
- `swagbounties.admin` grants full access including reading anonymous bounty creators via `/bountyadmin inspect`.
